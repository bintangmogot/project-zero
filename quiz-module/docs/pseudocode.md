<!-- 
1. create and prepare for the objects 
    - quiz material (question, answers, & correction)
    - start button
    - restart button
    - prev and next question button
    - next quiz button (if the first one complete with score > 3)
    - container for quiz
    - current question, max question, current score
2. pick all the objects to javascript
3. put first cover page with start button
4. show the next page (quiz page)
    - there are quiz material, prev and next question
    - if it's the first page, only show the next button (user can skip the question)
    - auto next page if user click answer button
    - if it's the last question, go to result page
5. show the result page
    - there are restart and next quiz button if score > 3, else only restart button
    - show the label based on the user score
 -->